{
  "builders": [{
    "type": "docker",
    "image": "nikcbg/xenial64",
    "commit": "true"
  }],
 "provisioners": [
    {
      "type": "shell",
      "inline": [
        "sudo apt-get -y update",
        "sudo apt-get -y install -y nginx"
      ]
    }
  ],
  "post-processors": [
    [
      {
        "type": "docker-tag",
        "repository": "nginx64",
        "tag": "0.1"
      }
    ]
  ]
}
