---
driver:
  name: docker
  name: docker
  use_sudo: false
  privileged: true
  run_command: /usr/sbin/init
  
provisioner:
  name: shell

platforms:
  - name: nginx
    image: nginx:0.1
        
verifier:
  name: inspec

suites:
  - name: default
    run_list: []
