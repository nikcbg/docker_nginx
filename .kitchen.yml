---
driver:
  name: docker

provisioner:
  name: shell

platforms:
  - name: default-nginx
    driver:
      box: nginx:0.1
      provider: xenial64
 
verifier:
  name: inspec

suites:
  - name: default
